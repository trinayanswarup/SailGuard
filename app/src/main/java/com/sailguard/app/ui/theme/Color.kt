package com.sailguard.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Saily-inspired light palette ─────────────────────────────────────────────
val TealPrimary    = Color(0xFF00C2B2)   // primary accent
val TealLight      = Color(0xFF33D4C8)
val TealDark       = Color(0xFF009A8D)
val TealSurface    = Color(0xFFE0FAF8)   // teal-tinted surface for chips/badges

// Backgrounds
val AppBackground  = Color(0xFFF7F8FA)   // page background
val AppSurface     = Color(0xFFFFFFFF)   // card / bottom nav background
val AppSurface2    = Color(0xFFF3F4F6)   // secondary surface, disabled inputs

// Text
val TextPrimary    = Color(0xFF0D1117)   // headings / body
val TextSecondary  = Color(0xFF6B7280)   // sub-labels
val TextMuted      = Color(0xFF9CA3AF)   // placeholder / disabled

// Borders
val CardBorder     = Color(0xFFE5E7EB)

// Status
val SuccessGreen   = Color(0xFF10B981)
val WarningAmber   = Color(0xFFF59E0B)
val ErrorRed       = Color(0xFFEF4444)
val InfoBlue       = Color(0xFF3B82F6)

