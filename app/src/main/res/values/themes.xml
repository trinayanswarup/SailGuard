<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.SailGuard" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.SailGuard.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.SailGuard</item>
    </style>

</resources>
